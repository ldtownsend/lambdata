"""
lambdata - a collection of data science helper functions
"""

import pandas as pd
import numpy as np

#sample code

# Using all caps because these are "global variables", that is a common convention
ONES = pd.DataFrame(np.ones(10))
ZEROS = pd.DataFrame(np.zeros(50))
